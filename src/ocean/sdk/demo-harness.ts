/**
 * Entry for /ocean/harness/demo.html — the reference harness page every
 * scene agent copies. Built to /assets/ocean/harness/demo.js by tools/build.ts.
 */

import { demoScene } from "./demo-scene.ts";
import { runHarness } from "./harness.ts";

runHarness(demoScene);
