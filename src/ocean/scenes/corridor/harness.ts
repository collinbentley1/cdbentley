/**
 * Entry for /ocean/harness/corridor.html — built to
 * /assets/ocean/harness/corridor.js by tools/build.ts (auto-discovered).
 */

import { runHarness } from "../../sdk/index.ts";
import { scene } from "./scene.ts";

runHarness(scene);
