/**
 * Entry for /ocean/harness/kitchen-table.html — built to
 * /assets/ocean/harness/kitchen-table.js by tools/build.ts.
 */

import { runHarness } from "../../sdk/index.ts";
import { scene } from "./scene.ts";

runHarness(scene);
