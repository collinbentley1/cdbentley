/**
 * Entry for /ocean/harness/anglerfish.html — built to
 * /assets/ocean/harness/anglerfish.js by tools/build.ts.
 */

import { runHarness } from "../../sdk/index.ts";
import { anglerfishScene } from "./scene.ts";

runHarness(anglerfishScene);
