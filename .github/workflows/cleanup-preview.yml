name: Cleanup preview

on:
  pull_request:
    types:
      - closed

permissions: {}

jobs:
  cleanup:
    permissions:
      id-token: write # Exchange only for the exact-SHA preview traffic operator.
    uses: collinbentley1/platform/.github/workflows/cleanup-preview.yml@8cefb91ab8d20c0bbfef7feab2d2b15d76785632
